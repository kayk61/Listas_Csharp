using System;

namespace lista3;
class Program
{


    static void Main(String[] args)
    {
        int opcao = 9;

        switch (opcao)
        {
            case 1: exercicio1(); break;
            case 2: exercicio2(); break;
            case 3: exercicio3(); break;
            case 4: exercicio4(); break;
            case 5: exercicio5(); break;
            case 6: exercicio6(); break;
            case 7: exercicio7(); break;
            case 8: exercicio8(); break;
            case 9: exercicio9(); break;
            case 10: exercicio10(); break;

        }

        static void exercicio1()
        {

            Console.WriteLine("digite dois numeros e sera dito se o primero e maior que o segundo");
            double num = double.Parse(Console.ReadLine()!);
            double num2 = double.Parse(Console.ReadLine()!);

            if (num > num2)
            {
                Console.WriteLine("O primeiro numero é maior que o segundo");

            } else
            {
                Console.WriteLine("O primeiro numero não é maior que o segundo");
            }
        }
        static void exercicio2()
        {

            Console.WriteLine("digite dois numeros inteiros e sera dito se o primero é menor que o segundo");
            int num = int.Parse(Console.ReadLine()!);
            int num2 = int.Parse(Console.ReadLine()!);

            if (num < num2)
            {
                Console.WriteLine("O primeiro numero é menor que o segundo");

            }
            else
            {
                Console.WriteLine("O primeiro numero não é menor que o segundo");
            }
        }

        static void exercicio3()
        {
            Console.WriteLine("Digite dois numeros e será exibido se eles são iguais");
            double num = double.Parse(Console.ReadLine()!);
            double num2 = double.Parse(Console.ReadLine()!);
            if (num == num2)
            {
                Console.WriteLine("Os numeros são iguais");
            } else
            {
                Console.WriteLine("Os numeros são diferentes");
            }


        }
        static void exercicio4()
        {
            Console.WriteLine("Digite tres numeros inteiros e sera dito se o primeiro é menor que o segundo numero e se é maior q o terceiro");
            int num = int.Parse(Console.ReadLine()!);
            int num2 = int.Parse(Console.ReadLine()!);
            int num3 = int.Parse(Console.ReadLine()!);

            if (num < num2 && num > num3)
            {
                Console.WriteLine("O primeiro numero é menor que o segundo e é maior que o terceiro");
            
            } else if (num > num2 && num < num3)
            {

                Console.WriteLine("O primeiro numero nao e menor que o segundo nem maior que o terceiro");
            }
            if (num > num2 && num > num3)
            {
                Console.WriteLine("O primeiro numero é maior que o segundo e é maior que o terceiro");

            }
            else if (num < num2 && num < num3)
            {

                Console.WriteLine("O primeiro numero é menor que o segundo e menor que o terceiro");
            }
            else if (num == num2 && num == num3)
            {
                Console.WriteLine("todos os numeros são iguais");
            }else
            {
                Console.WriteLine("");
            }
        }
        static void exercicio5()
        {
            Console.WriteLine("Digite um numero com ponto flutuante e sera exibido se ele é maior ou igual o segundo");
            float num = float.Parse(Console.ReadLine()!);
            float num2 = float.Parse(Console.ReadLine()!);

            if (num == num2)
            {
                Console.WriteLine("O numero é igual ao primeiro");
            }
            else if (num > num2)
            {
                Console.WriteLine("O primeiro numero é maior que o segundo");
            }else
            {
                Console.WriteLine("O primeiro numero é menor que o segundo");
            }


        }
        static void exercicio6()
        {
            Console.WriteLine("Digite um numero inteiro e será dito se ele é impar ou par");
            int num = int.Parse(Console.ReadLine()!);
            
            if ( num % 2 == 0 )
            {
                Console.WriteLine("O numero é par");
            }else
            {
                Console.WriteLine("O numero é impar");
            }
        }
        static void exercicio7()
        {
            Console.WriteLine("Digite um numero com ponto flutuante e sera exibido se ele é maior ou igual o segundo");
            float num = float.Parse(Console.ReadLine()!);
            float num2 = float.Parse(Console.ReadLine()!);

            if (num == num2)
            {
                Console.WriteLine("O numero é igual ao primeiro");
            }
            else if (num > num2)
            {
                Console.WriteLine("O primeiro numero é maior que o segundo");
            }
            else
            {
                Console.WriteLine("O primeiro numero é menor que o segundo");
            }
        }
        static void exercicio8()
        {
            Console.WriteLine("Digite um numero e sera exibido se ele é positivo ou negativo");
            double num = double.Parse(Console.ReadLine()!);
            if (num < 0)
            {
                Console.WriteLine("o numero digitado é negativo");

            } else
            {
                Console.WriteLine("O numero é positivo");
            }
        }
        static void exercicio9() {

            Console.WriteLine("Digite dois numeros e sera exibido se a diferença entre eles é menor ou igual a 10");
            int num = int.Parse(Console.ReadLine()!);
            int num2 = int.Parse(Console.ReadLine()!);

            if (-1*(num - num2) < 10)
            {
                Console.WriteLine("a diferença entre eles é menor que 10");
            }else if ( -1*(num - num2) == 10) 
            {
                Console.WriteLine("A diferença entre os numeros é igual a 10"); 
            }else
            {
                Console.WriteLine("a diferença é maior que 10");
            }

        }
        static void exercicio10()
        {
            Console.WriteLine("Digite um numero e sera exibido se ele é igual a 0");
            float num = float.Parse(Console.ReadLine() !);
            if (num == 0)
            {
                Console.WriteLine("O numero digitade é igual a 0");
            }else
            {
                Console.WriteLine("O numero digitado não é igual a 0");
            }
        }
    }
}
